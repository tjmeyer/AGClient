/// @description Insert description here
// You can write your code in this editor
if(isSelected)
{
	draw_set_color(c_red);
	draw_set_alpha(1);
	draw_circle(x, y, sprite_get_width(sprite_index) * image_xscale / 2, true);
}